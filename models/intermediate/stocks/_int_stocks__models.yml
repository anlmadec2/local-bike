models:
  - name: int_stocks_joined_to_stores_and_products
    description: "Stock details with stores and products."
    columns:
      - name: stock_id
        description: "Primary key for this table."
        tests:
          - unique
          - not_null